<UserControl 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="FR73.Tabs.EntriesTab"
    MinWidth="400" MinHeight="300">
    <Grid x:Name="EntriesRoot" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
        <ToolBarTray Grid.Row="0">
            <ToolBar>
                <Button Name="AddBtn">Add</Button>
                <Button Name="ClearBtn">Clear</Button>
                <Button Name="UpdateBtn">Update</Button>
            </ToolBar>
        </ToolBarTray>
        <Grid Name="StammdatenView" Grid.Row="1">
        </Grid>
    </Grid>
</UserControl>
